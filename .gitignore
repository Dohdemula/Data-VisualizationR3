# Dependencies
node_modules/

# OS
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.suo
*.user

# Environment - never commit; see .env.example in each subfolder
.env
.env.local
.env.*.local

# SQLite database files -never commit live data
server/data/*.db
server/data/*.db-shm
server/data/*.db-wal

# Dataset -large CSV, not source code
Data set/

# Server runtime logs
server/*.log

# Temp files
*.tmp
*.cookie
cookies.txt
sow_text.txt

# Claude Code - project diary, not for the repo
CLAUDE.md

#Scope of work frontend - not for the repo
Scope_of_Work_Dashboard_Frontend.docx